<UserControl x:Class="CustomizableTablesWithDeadlines.Views.TablesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="{StaticResource PagePadding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="{Binding Strings.CreateNewTable}" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0"
                    Command="{Binding CreateTableAsyncCommand}" />
            <Button Content="{Binding Strings.ImportFromWord}" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"
                    Command="{Binding ImportFromWordCommand}" />
            <Button Content="{Binding Strings.RenameTable}" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"
                    Command="{Binding RenameTableCommand}" />
            <Button Content="{Binding Strings.DeleteTable}" Style="{StaticResource DangerButton}"
                    Command="{Binding DeleteTableCommand}" />
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Tables}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardBorder}" Width="320" Margin="0,0,16,16">
                            <Border.InputBindings>
                                <MouseBinding MouseAction="LeftClick"
                                              Command="{Binding DataContext.SelectTableCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                              CommandParameter="{Binding}" />
                            </Border.InputBindings>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,12" />
                                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Style="{StaticResource MutedText}">
                                        <Run Text="{Binding DataContext.Strings.Columns, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=OneWay}" />
                                        <Run Text=": " />
                                        <Run Text="{Binding ColumnCount, Mode=OneWay}" />
                                    </TextBlock>
                                    <TextBlock Style="{StaticResource MutedText}" Margin="16,0,0,0">
                                        <Run Text="{Binding DataContext.Strings.Rows, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=OneWay}" />
                                        <Run Text=": " />
                                        <Run Text="{Binding RowCount, Mode=OneWay}" />
                                    </TextBlock>
                                </StackPanel>
                                <TextBlock Grid.Row="2" Style="{StaticResource MutedText}" Margin="0,0,0,16">
                                    <Run Text="{Binding DataContext.Strings.LastUpdated, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=OneWay}" />
                                    <Run Text=": " />
                                    <Run Text="{Binding LastUpdated, StringFormat=g, Mode=OneWay}" />
                                </TextBlock>
                                <StackPanel Grid.Row="3" Orientation="Horizontal">
                                    <Button Content="{Binding DataContext.Strings.Open, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            Style="{StaticResource PrimaryButton}" Margin="0,0,8,0"
                                            Command="{Binding DataContext.OpenTableCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}" />
                                    <Button Content="{Binding DataContext.Strings.RenameTable, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"
                                            Command="{Binding DataContext.RenameTableItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}" />
                                    <Button Content="{Binding DataContext.Strings.Delete, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            Style="{StaticResource DangerButton}"
                                            Command="{Binding DataContext.DeleteTableItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <TextBlock Grid.Row="1" Text="{Binding Strings.NoTablesYet}"
                   HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock.Style>
                <Style TargetType="TextBlock" BasedOn="{StaticResource MutedText}">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Tables.Count}" Value="0">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </Grid>
</UserControl>
